# PhotoDB web UI

Angular 22 companion for browsing a PhotoDB library and scanning local folders
with client-side duplicate detection. See [`../docs/WEB_UI.md`](../docs/WEB_UI.md)
for architecture, development commands, and the fixture acceptance check.

```bash
npm ci
npm start
npm test -- --watch=false
npm run build
```
